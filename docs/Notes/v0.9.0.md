3. تعديل الكيانات

بعدها نجعل جميع الكيانات ترث منه.
بدلاً من:

public class Mosque {

    private UUID id;

    private LocalDateTime createdAt;

    private LocalDateTime updatedAt;

}

تصبح:

@Entity
@Table(name = "mosques")
public class Mosque extends BaseEntity {

    ...
}
=============================================================================
User
Mosque
Donation
DonationCategory
MosqueMembership
        ↓
extends AuditableEntity

AuditLog
        ↓
extends BaseEntity
=============================================================================
الهيكل المقترح
common
└── exception
    │
    ├── BusinessException
    │
    ├── NotFoundException
    ├── AlreadyExistsException
    ├── ValidationException
    ├── ConflictException
    ├── UnauthorizedException
    └── ForbiddenException

ثم داخل كل Module:

modules
└── donations
    └── exception
        ├── DonationNotFoundException
        └── DonationAlreadyExistsException
=============================================================================
Controller
        │
        ▼
ApiResponseBuilder
        │
        ▼
Service
        │
        ▼
Repository
=============================================================================
في رأيي، بما أننا نبني مشروعًا احترافيًا، فيجب الفصل بين المسؤوليات.
✅ Seeder

وظيفته:

إنشاء البيانات الأساسية (Reference Data)
Roles
Mosque Positions
Permission Groups
Donation Categories
...

أي أنه لا يحتوي أي منطق (Business Logic).

✅ Initializer

وظيفته:

إنشاء البيانات التي تعتمد على خدمات النظام.

مثل:

Permissions
Position Permissions
Default Admin Permissions
...

أي أنه يستخدم Services وليس Repositories مباشرة.
=============================================================================
احذف جميع الجداول:

DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
=============================================================================
config
└── seed
    ├── RoleSeeder
    ├── MosquePositionSeeder
    ├── PermissionGroupSeeder
    ├── PermissionSeeder
    ├── PositionPermissionSeeder
    ├── DonationCategorySeeder
    └── ...
=============================================================================
الحزمة الثالثة: Query Layer
7. Pagination

تحويل جميع APIs التي تعيد قوائم إلى:

Page<T>
8. Search & Filtering

مثلاً:

GET /donations

?page=0
&size=20
&status=PENDING
&type=FOOD
&mosque=...

وأقترح تصميم Search Criteria موحد منذ الآن.

9. Sorting
sort=createdAt,desc
الحزمة الرابعة: Infrastructure
10. ModuleInitializer

هذه من أفضل الأفكار في الخطة.

بدلاً من:

PermissionInitializer

يصبح لدينا:

ModuleInitializer

ثم:

AuthorizationInitializer

MosqueInitializer

DonationInitializer

VolunteerInitializer

ويتم تنفيذها حسب الترتيب.

11. Swagger

إضافته الآن قرار ممتاز.

12. Base Tests

إنشاء:

BaseIntegrationTest

BaseControllerTest

BaseServiceTest
13. Validation

إنشاء Validators مخصصة مثل:

@ExistingMosque

@ExistingDonationCategory

@ExistingUser
14. Cleanup

تنظيف المشروع بالكامل.
=============================================================================
بعد نجاح التشغيل

سنبدأ مباشرة Phase 2 من Shared Infrastructure

Searching + Filtering

وهذه ستفيد جميع Modules مستقبلاً.

بدلاً من

GET /api/v1/mosques

سيصبح لدينا

GET /api/v1/mosques?page=0

أو

GET /api/v1/mosques?size=20

أو

GET /api/v1/mosques?sort=name

أو

GET /api/v1/mosques?sort=name,desc

وأيضًا

GET /api/v1/mosques?search=محمد

أو

GET /api/v1/mosques?city=مكناس

أو

GET /api/v1/mosques?country=Morocco

وسيصبح لدينا API احترافي مثل GitHub وStripe وSpring Data REST.

خطة المرحلة التالية

سنبني مكونات عامة قابلة لإعادة الاستخدام:

✅ SearchRequest
✅ SpecificationBuilder
✅ Filter
✅ SearchCriteria
✅ دعم Spring Data JPA Specifications
✅ تطبيق البحث أولاً على Mosque Module
✅ ثم إعادة استخدامه لاحقًا في التبرعات والمستخدمين وبقية الوحدات
=============================================================================
ممتاز، وهذا يؤكد أننا متوافقان على التسلسل. بما أن **اليوم الثامن** انتهى بالفعل بهذا الشكل، فلا داعي لإعادة بناء Donation Module. نحن الآن جاهزون للانتقال إلى **اليوم التاسع**.

## DAY 09 - إدارة أعضاء التبرعات (Donation Workflow & Assignment)

الهدف في هذا اليوم هو الانتقال من مجرد تسجيل التبرع إلى **إدارة دورة حياته داخل المسجد**.

بدلاً من أن يكون التبرع مجرد سجل في قاعدة البيانات، سنجعله يمر عبر مراحل تشغيلية يديرها أعضاء المسجد.

---

# المرحلة 1: إنشاء DonationAssignment

سنضيف كيانًا جديدًا يربط التبرع بعضو من أعضاء المسجد المسؤول عن معالجته.

```
DonationAssignment

UUID id

Donation donation

MosqueMembership assignee

assignedAt

notes

active
```

الغرض منه:

* من استلم التبرع؟
* من كُلّف بتوزيعه؟
* من يتابعه؟

---

# المرحلة 2: DonationHistory

سننشئ سجلًا لكل تغيير يطرأ على التبرع.

```
DonationHistory

UUID id

Donation donation

User actor

action

oldStatus

newStatus

notes

createdAt
```

أمثلة:

* Donation Created
* Donation Received
* Donation Allocated
* Donation Delivered
* Donation Cancelled

هذا السجل سيصبح أساسًا لصفحة النشاط (Activity Timeline).

---

# المرحلة 3: صلاحيات العمليات

ربط العمليات بالصلاحيات التي أنشأناها:

| العملية    | الصلاحية            |
| ---------- | ------------------- |
| إنشاء تبرع | `donation.create`   |
| تعديل      | `donation.update`   |
| حذف        | `donation.delete`   |
| استلام     | `donation.receive`  |
| تخصيص      | `donation.allocate` |
| تسليم      | `donation.deliver`  |
| إلغاء      | `donation.cancel`   |

---

# المرحلة 4: التحقق من انتقال الحالة

منع الانتقالات غير المنطقية.

المسموح:

```
PENDING
   │
   ▼
RECEIVED
   │
   ▼
ALLOCATED
   │
   ▼
DELIVERED
```

ويجوز الإلغاء في المراحل المناسبة حسب قواعد العمل.

أمثلة:

* ❌ `DELIVERED → PENDING`
* ❌ `PENDING → DELIVERED`
* ❌ `CANCELLED → RECEIVED`

---

# المرحلة 5: Dashboard

إضافة مؤشرات جديدة مثل:

* التبرعات بانتظار الاستلام.
* التبرعات قيد التوزيع.
* التبرعات المسلمة اليوم.
* التبرعات الملغاة.
* إجمالي القيمة التقديرية للتبرعات.

---

# المرحلة 6: اختبارات التكامل

اختبار السيناريو الكامل:

1. إنشاء مسجد.
2. إنشاء تبرع.
3. استلام التبرع.
4. تخصيصه لعضو.
5. تسليمه.
6. التحقق من تحديث الحالة.
7. التحقق من إنشاء سجل في `DonationHistory`.
8. التحقق من تحديث لوحة المعلومات.

---

## النتيجة المتوقعة بنهاية اليوم التاسع

سنكون قد انتقلنا من مجرد **CRUD للتبرعات** إلى **محرك تشغيل متكامل للتبرعات** يعتمد على:

* ✅ دورة حياة واضحة (Workflow).
* ✅ تعيين المسؤولين عن التنفيذ.
* ✅ سجل تاريخي كامل لكل عملية.
* ✅ تكامل مع نظام الصلاحيات.
* ✅ تكامل مع لوحة المعلومات.

بعد ذلك سيكون المشروع مهيأً للانتقال إلى وحدات مثل **المبادرات (Initiatives)** أو **التطوع (Volunteer)**، مع إعادة استخدام نفس الأنماط المعمارية التي أثبتت نجاحها في وحدة التبرعات.

=============================================================================
هذه الخطوة هي التي ستجعل بإمكاننا تنفيذ استعلامات مثل:

GET /api/v1/mosques?city=Meknes&active=true

أو

GET /api/v1/donations?status=PENDING&type=FOOD

بدون كتابة Repository جديد لكل حالة.
=============================================================================
ثم يجب تعديل جميع Repositories التي نريد دعم البحث فيها

مثلاً:

public interface MosqueRepository extends
        JpaRepository<Mosque, UUID>,
        JpaSpecificationExecutor<Mosque> {
}

ولا تنس إضافة:

import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
=============================================================================
بعد نجاح هذه الخطوة سنبني SpecificationBuilder، وهو المسؤول عن دمج عدد غير محدود من SearchCriteria في Specification واحدة، مما يسمح بطلبات مثل:

GET /api/v1/mosques?country=Morocco&city=Meknes&name=محمد

أو:

GET /api/v1/donations?status=PENDING&type=FOOD

وسيكون هذا هو المحرك العام الذي ستستخدمه جميع وحدات منصة محسنون دون أي تكرار للكود.
=============================================================================
النتيجة

من دون أي تعديل مستقبلاً سيصبح الـ API يدعم تلقائياً:

GET /api/v1/mosques?page=0
GET /api/v1/mosques?page=0&size=20
GET /api/v1/mosques?sort=name,desc

وعندما نربط الفلاتر لاحقاً:

GET /api/v1/mosques?country=Morocco
GET /api/v1/mosques?city=Meknes
GET /api/v1/mosques?search=محمد

بل وحتى:

GET /api/v1/mosques?page=1
    &size=10
    &sort=name,asc
    &country=Morocco
    &city=Meknes

كل ذلك بنفس الخدمة ونفس الـ Repository.
=============================================================================
والنتيجة

الآن أصبح المشروع يدعم مباشرة:

GET /api/v1/mosques?country=Morocco

أو

GET /api/v1/mosques?city=Meknes

أو

GET /api/v1/mosques?country=Morocco&city=Meknes

أو

GET /api/v1/mosques?page=0&size=10&sort=name,desc&country=Morocco

بدون كتابة أي Repository إضافي.
=============================================================================
shared
└── query
    ├── request
    │     ├── SearchRequest.java
    │     ├── FilterRequest.java
    │     └── PaginationRequest.java
    │
    ├── response
    │     └── PageResponse.java
    │
    ├── criteria
    │     ├── SearchCriteria.java
    │     └── FilterOperator.java
    │
    ├── specification
    │     ├── GenericSpecification.java
    │     ├── SpecificationBuilder.java
    │     └── SearchSpecification.java
    │
    ├── parser
    │     └── SearchParser.java
    │
    └── utils
          └── QueryUtils.java
=============================================================================
package com.mohsinon.shared.query.criteria;

import lombok.AllArgsConstructor;
import lombok.Data;
import lombok.NoArgsConstructor;

@Data
@NoArgsConstructor
@AllArgsConstructor
public class SearchCriteria {

    private String key;

    private FilterOperator operator;

    private Object value;

    private Object valueTo;

}
=============================================================================
اطلعت على الهيكل بالكامل، وأرى أننا وصلنا إلى نقطة مهمة في المشروع.

**التقييم:** البنية الحالية جيدة، لكن يوجد بعض التكرار وعدم الفصل بين المسؤوليات. إذا استمررنا بهذا الشكل، فسيصبح من الصعب إضافة الوحدات القادمة (Volunteer, Projects, Marketplace...).

أقترح أن نعتمد بنية احترافية مستقرة من الآن.

## 1. الهيكل المقترح

```
shared
└── query
    ├── criteria
    │   ├── SearchCriteria.java
    │   └── FilterOperator.java
    │
    ├── parser
    │   └── SearchParser.java
    │
    ├── request
    │   ├── SearchRequest.java
    │   ├── PaginationRequest.java
    │   └── FilterRequest.java
    │
    ├── response
    │   └── PageResponse.java
    │
    ├── specification
    │   ├── GenericSpecification.java
    │   ├── SpecificationBuilder.java
    │   └── SearchSpecificationFactory.java
    │
    └── utils
        ├── PaginationUtils.java
        └── SearchUtils.java
```

وهذا بالفعل قريب جدًا مما وصلت إليه.

---

# 2. احذف SearchOperation

وجود

```java
SearchOperation
```

لم يعد له أي فائدة.

لأننا أصبحنا نستعمل

```java
FilterOperator
```

وهو أشمل.

إذن احذف

```
SearchOperation.java
```

نهائياً.

---

# 3. SearchRequest

لا نحتاج إطلاقاً إلى هذه الأسطر القديمة

```java
// private String key;
// private SearchOperation operation;
// private Object value;
```

احذفها.

ويبقى فقط

```java
@Getter
@Setter
public class SearchRequest extends PaginationRequest {

    private String search;

    private FilterRequest filters = new FilterRequest();

}
```

---

# 4. SearchParser

جيد جداً.

لكن سأضيف له قريباً دعم

```
BETWEEN

IN

NULL

NOT NULL
```

حتى يصبح مثل Spring Data REST.

---

# 5. GenericSpecification

أراه جيداً جداً.

ولا أنصح بتعديله حالياً.

سنضيف له لاحقاً فقط

```
Date

UUID

Boolean

Enum

Nested Fields

Join

Collection

```

---

# 6. SearchUtils

حالياً

```java
SearchUtils
```

لا يقدم إلا

```java
builder()
```

وسيتحول لاحقاً إلى

```
buildSpecification()

buildPageable()

buildPageResponse()

```

إذن اتركه كما هو.

---

# 7. PaginationUtils

ممتاز.

لن نغيره.

---

# 8. SearchSpecificationFactory

سيصبح مستقبلاً المسؤول الوحيد عن إنشاء الـ Specification.

أي أننا سنستعمل

```java
SearchSpecificationFactory.build(params)
```

بدلاً من

```java
new SpecificationBuilder()
```

في الـ Services.

---

# 9. الخطوة القادمة

الآن انتهينا من **Shared Query Infrastructure**.

وهذه البنية ستعمل مع جميع Modules.

---

## المرحلة التالية مباشرة

سنطبقها على أول Module.

وهو

```
Mosque Module
```

بحيث يتحول

```java
GET /api/v1/mosques
```

إلى

```http
GET /api/v1/mosques?page=0
```

ثم

```http
GET /api/v1/mosques?page=0&size=20
```

ثم

```http
GET /api/v1/mosques?sort=name
```

ثم

```http
GET /api/v1/mosques?sort=name,desc
```

ثم

```http
GET /api/v1/mosques?city=Meknes
```

ثم

```http
GET /api/v1/mosques?country=Morocco
```

ثم

```http
GET /api/v1/mosques?name=~محمد
```

ثم

```http
GET /api/v1/mosques?capacity=>500
```

ثم

```http
GET /api/v1/mosques?capacity=>500&city=Meknes
```

وبذلك يصبح لدينا نظام بحث احترافي قابل لإعادة الاستخدام في جميع وحدات منصة **محسنون** دون كتابة أي منطق بحث جديد لكل وحدة. هذه ستكون أول خطوة فعلية للاستفادة من البنية التي أنشأناها.

=============================================================================
الآن اتضح السبب بشكل قاطع، وهذا يؤكد ما كنا نخطط له في الأيام السابقة.

## ما الذي يحدث فعلاً؟

في `AuthService` لديك:

```java
Role userRole = roleRepository.findByName("USER")
        .orElseThrow(() -> new RoleNotFoundException("USER"));
```

لاحظ أنه **لا ينشئ الدور**، وإنما **يفترض أن دور USER موجود مسبقًا**.

إذن عند حذف جدول `roles`، تصبح النتيجة:

```
roles
--------
(فارغ)
```

ثم عند أول تسجيل:

```
findByName("USER")
```

فيرجع:

```
Optional.empty()
```

ثم يرمي:

```
RoleNotFoundException
```

---

# لماذا لم نلاحظ هذا في الأيام الأولى؟

لأن قاعدة البيانات لم تكن تُحذف.

التسلسل كان:

```
اليوم 1
Hibernate
↓

أنشأ الجداول

↓

RoleSeeder (أو Insert يدوي)

↓

roles

ADMIN
USER
IMAM
...
```

بعد ذلك ظل الجدول موجودًا.

أما الآن فأنت حذفت جميع الجداول.

---

# أين المشكلة الحقيقية؟

ليست في `AuthService`.

وليست في `RoleRepository`.

المشكلة أن المشروع أصبح يحتوي على:

```
PermissionGroupSeeder

PermissionSeeder

PositionSeeder

...

ولا يحتوي على

RoleSeeder
```

أي أن أول Seeder يجب أن يعمل غير موجود.

---

# لكن هناك شيء أهم لاحظته

لدينا الآن:

```
RoleSeeder

PermissionGroupSeeder

PermissionSeeder

PermissionInitializer

...
```

وهذا بالضبط ما كنت أحاول الوصول إليه معك في نهاية اليوم التاسع.

وجود 10 أو 15 `CommandLineRunner` سيصبح فوضى مع مرور الوقت.

ولهذا اقترحت:

```
ModuleInitializer
```

وليس مجرد `PermissionInitializer`.

---

# ما الذي أنصح به الآن؟

بدلاً من هذا:

```
config
 └── seed
      RoleSeeder
      PermissionSeeder
      PermissionGroupSeeder
      PositionSeeder
      ...
```

أصبح أفضل أن تكون البنية:

```
shared
└── initializer
```

ثم:

```
ModuleInitializer
```

واجهة مشتركة:

```java
public interface ModuleInitializer {

    int getOrder();

    void initialize();

}
```

ثم:

```
AuthorizationInitializer
```

هو الذي ينشئ:

```
Roles

↓

Permission Groups

↓

Permissions

↓

Position Permissions
```

ثم:

```
MosqueInitializer
```

ثم:

```
DonationInitializer
```

ثم:

```
VolunteerInitializer
```

وفي النهاية يوجد Runner واحد فقط:

```
InitializerRunner
```

الذي ينفذ الجميع بالترتيب.

---

# لماذا هذا أفضل؟

لأن مشروع **محسنون** لن يتوقف عند:

* Users
* Mosques
* Donations

بل سيحتوي لاحقًا على:

* Volunteers
* Marketplace
* Courses
* Events
* Associations
* Inventory
* AI
* Notifications
* وغيرها.

ولو بقي لكل Module ثلاثة أو أربعة `CommandLineRunner` فسيصبح المشروع صعب الصيانة.

---

## هذه نقطة معمارية مهمة

أرى أن هذه هي اللحظة المناسبة لإيقاف إضافة أي Seeders جديدة، والانتقال مباشرة إلى **Infrastructure Sprint** الذي كنا قد خططنا له، وأول مهمة فيه هي **ModuleInitializer**.

بهذا ننهي مشكلة تكرار الـ Seeders مرة واحدة، ويصبح تشغيل مشروع محسنون من قاعدة بيانات فارغة يقوم تلقائيًا بتهيئة كل ما يحتاجه النظام بطريقة منظمة وقابلة للتوسع. وهذا يتماشى مع الاتجاه المعماري الذي بنيناه خلال الأيام التسعة الماضية.

=============================================================================
رأيي المعماري

بما أن مشروع محسنون منصة خيرية وإدارية، فإن البيانات يجب أن تكون قابلة للتتبع (Audit) ولا تُفقد بسهولة.

لذلك أوصي بأن نعتمد من الآن سياسة موحدة في المشروع:

✅ Users: Soft Delete
✅ Mosques: Soft Delete
✅ Donations: Soft Delete (أو Cancel حسب الحالة)
✅ Volunteer Opportunities: Soft Delete
✅ Projects: Soft Delete

ونجعل الحذف الحقيقي (DELETE FROM ...) مقتصرًا على أدوات الصيانة أو الإدارة العليا إذا احتجنا إليه.

هذا القرار سيجنبك كثيرًا من مشاكل العلاقات (Foreign Keys)، ويحافظ على تاريخ النظام، ويتوافق مع أفضل الممارسات في الأنظمة المؤسسية.
=============================================================================
//		لا أنصح بالحذف الحقيقي للمساجد. لأن المسجد يمثل بيانات تاريخية 
//	    mosqueRepository.delete(mosque);
=============================================================================
shared
│
├── entity
│   ├── BaseEntity.java
│   ├── AuditableEntity.java
│   └── LifecycleEntity.java
│
├── lifecycle
│   ├── Activatable.java
│   ├── Archivable.java
│   ├── SoftDeletable.java
│   ├── LifecycleService.java
│   ├── LifecycleUtils.java
│   └── LifecycleException.java
=============================================================================
الاختبارات التي لم تعمل
Delete Mosque
DELETE /api/v1/mosques/{id}




لا أنصح بالحذف الحقيقي (DELETE) للمساجد.
=============================================================================
lifecycleService.deactivate(entity);
=============================================================================
BaseEntity
│
├── id
├── createdAt
└── updatedAt

        │

AuditableEntity
│
├── createdBy
├── updatedBy
└── deletedBy

        │

LifecycleEntity
│
├── active
├── archived
├── archivedAt
├── archivedBy
├── deleted
└── deletedAt
=============================================================================
لكنني أقترح تحسينًا معماريًا

بما أننا بدأنا ببناء Shared Infrastructure، فمن الأفضل أن نجعل جميع الكيانات ترث من LifecycleEntity بدل AuditableEntity.

عندها كل الكيانات ستحتوي على الحقل deleted، ويمكن تبسيط build() إلى:

public Specification<T> build() {

    return specification.and(
            (root, query, cb) ->
                    cb.isFalse(root.get("deleted")));
}

بدون try/catch، ويصبح النظام أكثر اتساقًا ونظافة. وهذا هو الاتجاه الذي أنصح به لمنصة محسنون على المدى الطويل.
=============================================================================
مثلاً: MosqueController.java

@DeleteMapping("/{id}")
public ResponseEntity<ApiResponse<Void>> delete(
        @PathVariable UUID id) {

    mosqueService.delete(id);

    return ResponseEntity.ok(
            ApiResponse.success(
                    "Mosque deleted successfully."
            )
    );
}

فتصبح النتيجة:

{
    "success": true,
    "message": "Mosque deleted successfully."
}
=============================================================================

=============================================================================

=============================================================================

=============================================================================

=============================================================================

